

/**
 * @file globalStyles.ts
 * @author Brendan Dileo, May 2025
 * Global styles to be applied accross the app.
 */

import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
});

export default globalStyles;